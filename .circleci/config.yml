version: 2.1
jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: make test-all
      - "slack/status":
          fail_only: false
  nqueen:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: make test-nqueen
      - "slack/status":
          fail_only: false
  dp:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: make test-dp
      - "slack/status":
          fail_only: false

orbs:
  slack: circleci/slack@3.4.2
workflows:
  wfs:
    jobs:
      - test
