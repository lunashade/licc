version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: make
      - "slack/status":
          fail_only: false

orbs:
  slack: circleci/slack@3.4.2
workflows:
  wfs:
    jobs:
      - build
